Simple trading example project to compare with xtrade, the Scala counterpart. 

ZTrade uses JUnit 4, Spring 3.0, javax.inject (JSR-330), SLF4J + Logback, and an absolute minimum of XML (only for testing).
